name: Build C++

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            name: Linux
          - os: macos-latest
            name: macOS
          - os: windows-latest
            name: Windows

    runs-on: ${{ matrix.os }}
    name: Build (${{ matrix.name }})

    steps:
    - uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Install Dependencies (Linux)
      if: runner.os == 'Linux'
      run: |
        sudo apt-get update
        sudo apt-get install -y cmake build-essential

    - name: Install Dependencies (macOS)
      if: runner.os == 'macOS'
      run: |
        brew install cmake

    # Note: Protobuf disabled for standalone builds (session_proto not available)
    # Full protobuf builds happen in the parent session repo's minitest workflow
    - name: Configure
      shell: bash
      run: |
        cmake -B build -DENABLE_PROTOBUF=OFF -DCMAKE_BUILD_TYPE=Release

    - name: Build Project
      shell: bash
      run: |
        cmake --build build --config Release -j4

    - name: Run Tests
      shell: bash
      run: |
        cd build
        ctest --output-on-failure -C Release
